// Standalone Catch2 entry point for the BambuPlotter test suite
// (plotter_tests target). Kept separate from libslic3r_tests so the plotter
// safety-critical tests build and run even when the upstream test suite does
// not compile.
#include <catch_main.hpp>
